tags:
  - authors
parameters:
  - in: path
    name: author_id
    required: true
    type: string
responses:
  200:
    description: OK
    schema:
      $ref: '#/components/schemas/Author'
  404:
    description: Not found
