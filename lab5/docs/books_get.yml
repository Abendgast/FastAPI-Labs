tags:
  - books
parameters:
  - in: path
    name: book_id
    required: true
    type: string
responses:
  200:
    description: OK
    schema:
      $ref: '#/components/schemas/Book'
  404:
    description: Not found
